<li id="search">
	{{#if showCategory}}
		<small><a {{action 'showTerm' category.id}}>{{category.name}}</a></small><br>
	{{/if}}
	<a class="title" {{action 'showPost' post.id}}>{{{post.title}}}</a>
	{{{excerptSafe}}}<br>
	<a  class="btn" {{action 'showPost' post.id}}>Seguir leyendo</a>
</li>